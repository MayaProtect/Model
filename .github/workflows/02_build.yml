name: Deploy to ECR

on:
 
  push:
    branches: [ main, develop ]

jobs:
  
  build:
    
    name: Build Image
    runs-on: ubuntu-latest

   
    steps:

    - name: Check out code
      uses: actions/checkout@v3
    
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_KEY }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_KEY }}
        aws-region: eu-west-1

    - name: Login to Amazon ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v1

    - name: Downcase repo name
      id: downcase-repo-name
      uses: ASzc/change-string-case-action@v1
      with:
        string: ${{ github.event.repository.name }}
    
    # - name: Build, tag, and push image to Amazon ECR
    #   env:
    #     REGISTRY: ${{ steps.login-ecr.outputs.registry }}
    #     REGISTRY_ALIAS: mayaprotect
    #     REPOSITORY: ${{ steps.downcase-repo-name.outputs.lowercase }}
    #     IMAGE_TAG: ${{ github.sha }}
    #   run: |
    #     docker build -t $REGISTRY/$REGISTRY_ALIAS/$REPOSITORY:$IMAGE_TAG -t $REGISTRY/$REGISTRY_ALIAS/$REPOSITORY:latest .
    #     docker push $REGISTRY/$REGISTRY_ALIAS/$REPOSITORY:$IMAGE_TAG
    #     docker push $REGISTRY/$REGISTRY_ALIAS/$REPOSITORY:latest
    - name: Build, tag, and push image to Amazon ECR
      run: |
        docker build -t test:1.0.0 -t test:latest .
        docker push 378810107815.dkr.ecr.eu-west-1.amazonaws.com/mayaprotect/test:1.0.0
        docker push 378810107815.dkr.ecr.eu-west-1.amazonaws.com/mayaprotect/test:latest
        